import DesignLabForgotPasswordClient from './client';

export const metadata = {
  title: 'Reset Password',
};

export default function DesignLabForgotPasswordPage() {
  return <DesignLabForgotPasswordClient />;
}
